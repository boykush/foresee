#!/usr/bin/env bash
#MISE description="Run Go tests"
#MISE arg "service" help="Service name (e.g., transaction). If not specified, runs all tests"

if [ -n "$1" ]; then
  cd "services/$1" || exit 1
  go test ./...
else
  go test ./services/...
fi
