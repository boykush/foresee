#!/usr/bin/env bash
#MISE description="Format Go code"
#MISE arg "service" help="Service name (e.g., transaction). If not specified, formats all services"

if [ -n "$1" ]; then
  cd "services/$1" || exit 1
  go fmt ./...
else
  go fmt ./services/...
fi
